/**
 * MCP server name DomainProof registers under. The actual tool/resource
 * surface (verify-domain-flow, etc.) lands once the CLI and MCP server
 * ship their real command surface.
 */
export const SERVER_NAME = 'domainproof'
