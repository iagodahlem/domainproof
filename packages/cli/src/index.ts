#!/usr/bin/env node

/**
 * DomainProof CLI entrypoint. Command surface (verify, status, init) lands
 * once the CLI ships its real commands — for now this only proves the
 * build/bin wiring end to end.
 */
export function main(): void {
  console.log("domainproof CLI — coming soon");
}

main();
